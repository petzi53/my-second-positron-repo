---
title: "Untitled"
format: html
editor:
  preview-type: external-browser
  render-on-save: true
---

This is a new line.

This is my second line. 

Another line to see if there are changes?

There is a fourth line.


```{r}

suppressPackageStartupMessages(library(tidyverse))
```

## Iris plot from AI


```{r}
ggplot(iris, aes(x = Sepal.Length, y = Petal.Length, color = Species)) +
  geom_point(size = 3, alpha = 0.7) +
  geom_smooth(method = "lm", se = TRUE, alpha = 0.2) +
  scale_color_brewer(palette = "Dark2") +
  labs(
    title = "Iris Flower Measurements",
    subtitle = "Petal length increases with sepal length across species",
    x = "Sepal Length (cm)",
    y = "Petal Length (cm)"
  ) +
  theme_light()
```

## Palmerpenguins from AI


```{r}
# Install palmerpenguins if needed
# install.packages("palmerpenguins")

library(palmerpenguins)

ggplot(penguins, aes(x = flipper_length_mm, y = body_mass_g, color = species)) +
  geom_point(size = 3, alpha = 0.8) +
  geom_smooth(method = "lm", se = FALSE, linewidth = 1.2) +
  scale_color_manual(
    values = c(
      "Adelie" = "#FF6F00",
      "Chinstrap" = "#C158DC",
      "Gentoo" = "#067476"
    )
  ) +
  labs(
    title = "Penguin Body Mass vs. Flipper Length",
    subtitle = "Linear relationships vary by species",
    x = "Flipper Length (mm)",
    y = "Body Mass (g)",
    color = "Species"
  ) +
  theme_minimal(base_size = 13) +
  theme(
    plot.title = element_text(face = "bold", size = 16),
    plot.subtitle = element_text(color = "gray40"),
    legend.position = "top"
  )
```